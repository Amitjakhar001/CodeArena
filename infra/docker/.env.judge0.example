# Template — the deploy script generates a real `.env.judge0` with random
# passwords on first run. Copy this only if you want to provide your own values.

JUDGE0_DB_PASSWORD=replace-with-a-real-password
JUDGE0_REDIS_PASSWORD=replace-with-a-real-password
